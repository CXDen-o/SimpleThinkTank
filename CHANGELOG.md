# Changelog

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [0.2.0] - 2026-07-28

### 新增

- 对话模型选择：分级推荐表（轻量 / 均衡 / 高配 / 旗舰 四档）+ 自动发现本机已安装模型直接选用；首启引导适配"已有本地模型免下载"场景
- 文档删除：连带清理切分片段与向量数据
- 设置入口扩展：知识库列表、文档页、聊天页均可直接打开设置窗口

### 修复

- 模型状态检测：已下载模型误报"未下载"（Ollama 未显式带 tag 拉取的模型实际存为 `:latest` 后缀），连带修复开始对话时设置窗口误自动弹出
- Ollama 安装 / 清理等子进程不再弹出 cmd 黑窗口（Windows `CREATE_NO_WINDOW`）
- 导入可靠性三连修：支持中途取消；启动时将卡死文档（解析中 / 向量化中 / 切分中）恢复为失败态；文档索引全程 SQLite 事务保证原子性
- 流式输出上翻阻力：滚动跟随改为非对称判定 + 方向感知回粘，上翻阅读历史即时释放、不再被逐 token 拉回底部
- localStorage 旧名残留键首启一次性迁移清理

## [0.1.0] - 2026-07-25

首个公开发布版本。

### 功能

- 知识库管理：创建 / 重命名 / 删除，支持运行时热切换（切换时自动放弃进行中查询，防串扰）
- 文档导入：PDF / DOCX / TXT / MD，支持文件夹批量导入与拖拽导入，进度条带 trickle 爬行动画
- 切分策略：固定长度 / 语义 / Agentic 智能切分（分析窗口可配置），切分结果可预览
- 向量存储：sqlite-vec 静态链接进 SQLite，单文件数据库，毫秒级 KNN 检索
- RAG 流式问答：检索增强生成，逐 token 流式输出（已关闭 thinking 模式，首 token 延迟大幅降低），引用来源可溯源
- 聊天智能滚动：流式输出贴底跟随，上翻自由阅读，"回到最新"按钮平滑追底
- Ollama 托管：自动检测 / 启动 / 退出确认；未安装时提供 winget + 官方安装包双通道静默安装引导
- 存储统计面板：文档 / 片段 / 向量 / 磁盘占用统计
- 设置：TopK / 温度 / 最大 Token / 代理 / 模型镜像源
